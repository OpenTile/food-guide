# Copy to .env for local development. .env is gitignored; this repository is public,
# so no real connection string or secret may be committed here.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/food_guide
PORT=3000
